version: "3.3"
services:
  app:
    build: .
    container_name: abyss
    volumes:
    - type: bind
      source: ~/abyss-data
      target: /data
    environment:
      - ABYSS_ENVIRONMENT=Production